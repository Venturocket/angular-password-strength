language: node_js
node_js:
  - 0.10

before_script:
  - npm install -g bower
  - bower install

#script:
#  - karma start --no-auto-watch --single-run --reporters dots --browsers PhantomJS
  
notifications:
  email: false
